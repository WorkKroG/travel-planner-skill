# Travel Planner — статус проекта

**Дата среза:** 2026-09-08

**Текущий этап:** одобренная пользователем компоновка дня Хаконэ перенесена в общий шаблон на ветке `codex/day-reference-layout` от `0b128b6` (слитый PR #10). Полный suite: **248 passed**. Независимое ревью исходного кода — **Ready for PR: yes**, открытых P0–P2 нет. Продукт остаётся source release candidate; слияние нового PR выполняет пользователь.

## Компоновка дня по одобренному примеру

Все 12 дней используют жёлтую шапку с тремя фактами, галереи с короткими подписями, крупные круглые иконки, заголовок «Как проведём день» и полные сценарии. Источники, авторство фотографий и дата проверки находятся в native disclosure внизу дня; независимая print-проекция сохраняет их при закрытом disclosure. На малом экране время переносится над текстом события, сценарии и фотографии складываются в колонку. Текстовые ссылки сохраняют область нажатия 44px.

В существующем bundle добавлены только необязательные аннотации представления: `period`, `icon`, `primary_label`, `primary_summary`, `primary_icon`. Renderer группирует соседние записанные периоды без сортировки и без догадок по часам, названиям или ID. Неопределённые по брони события JAL остаются без группы времени суток. После удаления новых полей Japan YAML полностью совпадает с исходным: все 242 события, URL, фото и их порядок сохранены. Два замечания ревью (размер области нажатия и пропущенные аннотации flow-style YAML) исправлены; добавлена проверка трёх групп во всех сценариях Хаконэ.

Свежие проверки: **248 tests passed**, Ruff, `pip check`, CLI help, JavaScript syntax, `git diff --check`, skill validation, staged plugin validation и создание source ZIP во временной папке. Статический Impeccable layout scan не выявил замечаний. В итоговом HTML независимо проверены уникальность ID, разрешимость ARIA/SVG/anchor references, порядок событий, URL и побайтное совпадение 18 встроенных фотографий.

Wheel собран со штатными build-зависимостями и установлен в чистую временную Python 3.12 environment. `pip check`, CLI help, временные `init`, `check`, `render` проходят; установленный пакет воспроизводит итоговый Japan HTML побайтно, а его HTML/CSS/JS/SVG совпадают с проверенными исходными assets.

Открываемый результат: [Japan itinerary HTML](../examples/japan-autumn-2026/outputs/itinerary.html), сборка с фиксированным `--at 2026-09-07T17:44:11+00:00`. SHA-256: `a348c53562959f4c024ba7036b3d8a49510c4e600400dfe316bd4484d24c1d58`. Новое направление и runtime workflow отражены в `DESIGN.md`, его sidecar и references. Браузерная, мобильная визуальная проверка и print preview остаются непроведёнными.

## Исторический редизайн 7 сентября

[Спецификация утверждённого направления](superpowers/specs/2026-09-07-html-visual-redesign-design.md) фиксирует жёлто-синий основной дизайн, «Абрикос и слива» как сохранённый запасной вариант, иконки активностей и фотографии в шапке дня. Пользователь отдельно одобрил одну широкую фотографию и три равных кадра; вариант с двумя фотографиями остаётся основным примером. На узком экране фотографии располагаются одной колонкой.

Дизайн применён ко всему общему шаблону: обложке, обзору, оглавлению, всем дням, фильтрам, подготовке, бюджету, открытым вопросам, рискам, источникам и печати. Галереи находятся в жёлтой шапке после вводного текста и перед компактными фактами. Иконки событий определяются `kind`; погодные и общие альтернативы используют записанную семантику, без специальных ID и индексов демонстрационного Хаконэ. Полные timelines, неизвестные значения, локальные альтернативы, RU/EN, native disclosures, ARIA tabs и no-JS сохраняются. Поиск и переключатель тем не добавлены.

До переноса сопоставлены текущие деревья `117c` и `7367`: данные родительской поездки совпадают с source после вычитания новых media records и photo sources. Пять канонических файлов, photo metadata и `sources.md` перенесены без изменения bytes. В media provenance сохранено всё содержимое; удалена только лишняя пустая строка в конце файла для whitespace check. Все 18 уникальных JPEG сверены по SHA-256 с исходным пользовательским HTML и новой сборкой. Количества по дням: `1,1,2,1,2,2,1,1,2,2,2,1`; Хаконэ содержит два фото, Мотосу остаётся в дне 8. Посторонние untracked материалы не перенесены; исходные деревья не менялись.

Свежие проверки: **241 tests passed** (Python 3.12), Ruff, `pip check`, CLI help, JavaScript syntax, `git diff --check`, skill frontmatter и staged plugin/ZIP validation. Новые тесты сначала выявили отсутствующие header-gallery, palette/layout и semantic-icon contracts, затем прошли после реализации. Статические проверки не являются browser layout или visual QA.

Открываемый артефакт: [Japan itinerary HTML](../examples/japan-autumn-2026/outputs/itinerary.html), детерминированная сборка с `--at 2026-09-07T17:44:11+00:00`. SHA-256: `20616b1fdd90300d19a8a146dabf765929d2855acf372394541d4321bde0bec2`.

Чистая установка wheel во временную Python 3.12 environment прошла `pip check`, CLI help, `init`, `check` и `render` вне source tree. Установленные HTML/CSS/JS/SVG совпадают с исходными assets; перенесённый Japan bundle создаёт тот же HTML побайтно. Staged plugin и ZIP проверены во временной папке.

Независимое техническое ревью дополнительно сверило данные с обоими исходными деревьями, воспроизводимость HTML, все 71 URL событий/альтернатив и 46 URL источников, уникальность ID и разрешимость ARIA/anchor/SVG references. Независимый Impeccable finish-review исходного кода — **Pass with limitations**, обязательных исправлений нет. Контраст конкретных пар рассчитан: основной текст/страница 12,16:1, вторичный/страница 7,90:1, вторичный/жёлтая шапка 6,47:1, белый/выбранная кнопка 8,31:1. Это не полный accessibility-аудит и не визуальное QA; layout, assistive technology и print preview требуют ручного просмотра.

## Историческая проверка галерей до редизайна

`days[].media` хранит до трёх локальных фотографий с подписью, alt, размерами, авторством, лицензией и разрешимой ссылкой на источник. Пустой список или отсутствие поля сохраняют валидный день без изображений. CLI воспроизводимо встраивает файлы из `media/` в общий HTML; обход каталога и symlink escape отклоняются. Каждая фотография сохраняет свой credit, а информация об импорте источника видна в HTML и `sources.md`. Широкий экран использует одну, две или три колонки по количеству фото; mobile — одну, print — максимум две и существующую опцию без изображений. Другие non-goals не изменены.

Для примера использован сохранённый перенос 12-дневного пользовательского маршрута Tokyo → Nikko → Izu → Hakone → Fuji → Tokyo из отдельной задачи. Все 18 JPEG извлечены из предоставленного HTML без изменения bytes; по дням: `1, 1, 2, 1, 2, 2, 1, 1, 2, 2, 2, 1`. Поиск и повторная загрузка источников не выполнялись. Происхождение и SHA-256 сохранены в [media/README.md](../examples/japan-autumn-2026/media/README.md). Фотографии имеют собственные лицензии и не становятся MIT-активами.

Полный suite галерей до редизайна после исправлений review: **237 passed**, Ruff, `pip check`, CLI help и `git diff --check` проходят. Тесты включают 0/1/2/3 изображения, лимит 3, schema/metadata, source references, локальные пути, symlink escape, специальные файлы/FIFO, перенос bundle, точные bytes HTML, escaping, no-JS/print contracts и неизменность фотографий. Единственный прежний Japan reference/hash обновлён осознанно из-за gallery CSS; пример отдельно сравнивается со свежей сборкой с фиксированным `--at 2026-09-07T17:44:11+00:00`.

Независимый reviewer сверил все 18 JPEG и metadata непосредственно с предоставленным HTML. Два подтверждённых P2 (зависание на FIFO и потеря видимого source ID в печатной подписи) воспроизведены regression tests и исправлены. Повторный технический verdict — **approve**, дополнительных подтверждённых замечаний нет. Этот результат не объявляется визуальной проверкой.

Wheel с окончательными исправлениями собран и установлен во временную чистую Python 3.12 environment; `pip check`, `init`, `check`, `render` проходят. Перенесённый Japan bundle воспроизводит тот же HTML из установленного wheel. Staged plugin validation, skill frontmatter validation, JS syntax и source archive smoke проходят; browser dependencies не добавлены. SHA-256 итогового Japan HTML: `5934e2f916df64548bd438062d171378d1ba2f80d10b1cb31adb14d24ac5def1`.

Встроенный браузер отклонил `file://` по URL security policy и явно запретил обход. В той задаче визуальные desktop/mobile/print observations не заявлены. Текущее поручение разрешает отдельный коммит и PR после свежих программных проверок и независимого ревью; пользовательская визуальная проверка нового файла остаётся непроведённой. Статические CSS tests не заменяют её.

## Актуальное решение

HTML-маршрут перестроен из dashboard-композиции в спокойный одноколоночный документ. Встроенное оглавление закрыто по умолчанию, каждый день читается как отдельная глава, а основной `days[].timeline` объединяет переезды, активности, еду, заселение, отдых и контрольные точки в одной хронологии.

Контекст принадлежит событию: HTTPS-ссылки и локальные замены хранятся в `timeline[].links[]` и `timeline[].alternatives[]`. Контрольная точка явно записывает, что проверить и как изменить план. `days[].scenarios[]` содержит только полноценные альтернативные timelines; основной план остаётся в `days[].timeline`. Все шесть типов событий используют отдельные SVG-иконки. JavaScript улучшает фильтры и ARIA tabs, но без него все сценарии и event-альтернативы остаются читаемыми. Полнотекстового поиска в документе нет. Print CSS восстанавливает скрытые дни и сценарии, отдельная print-only проекция сохраняет event-альтернативы независимо от состояния disclosure, каждый день начинается с новой страницы, а каждое печатное событие повторяет day/date/region-контекст. Полные URL источников и действий перечислены в печатном аппендиксе; на каждой странице повторяются provenance footer и номер.

`brief.yaml.document_language` выбирает английские или русские системные подписи, даты, weekday/enum/unknown labels и объявления JavaScript; отсутствие поля сохраняет английский default. Renderer не переводит пользовательский контент. Текущий 12-дневный Japan example записан по-русски и явно использует `document_language: ru`.

Канонический контракт описан в [PRODUCT.md](../PRODUCT.md), [ARCHITECTURE.md](../ARCHITECTURE.md) и [Readable Itinerary Days spec](superpowers/specs/2026-09-06-itinerary-readable-days-design.md). Старый HTML spec сохраняет требования к scope, lifecycle truth, accessibility, self-contained artifact и print, но больше не определяет навигацию, day rail или fragment-only backup.

## Предыдущая проверка readable-days (PR #9)

На предшествующем рабочем дереве readable-days были получены следующие результаты; они не являются проверками нынешних галерей:

- **190 tests passed** в основной Python 3.12 environment; clean-install и package smoke были отдельно подтверждены в чистой временной environment;
- Ruff, `pip check`, JavaScript syntax и `git diff --check` проходят;
- skill `quick_validate` и plugin validation из staged root `travel-planner` проходят;
- временные `init`, `check` и `render` проходят как из editable install, так и из собранного wheel;
- wheel содержит актуальные schemas, HTML template, CSS, JavaScript, icons и trip template;
- committed Japan HTML совпадает со свежим fixed-time render; SHA-256: `6f1ee3a9dee08289bbe42ded422dd9bed2ef7fa27906ced342113be8e42d15ed`;
- независимый skill forward-test завершён с `Pass`; Impeccable finish-review дал `Pass with limitations`; отдельный независимый code review повторно проверил exact head после исправлений и завершился с `Ready to merge: Yes`, без Critical или Important замечаний.

Статические тесты подтверждают source contracts, а не реальную браузерную раскладку или поведение assistive technology.

## Оставшиеся ручные и внешние gates

Локальный in-app browser отклонил `file://` artifact по URL security policy и запретил alternate-browser workaround. Поэтому desktop/mobile screenshots, фактический overflow на viewport, keyboard/screen-reader/zoom/contrast и print-preview observations на этой ветке не заявлены.

[RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) сохраняет непроходившие пользовательские проверки Chat web и mobile, а также внешнюю submission/review публичного каталога plugin. Восемь targeted prompts и три release scenarios остаются data-only и `not_executed`, без runner, judge или simulator. PR и его слияние не означают прохождение этих ручных или внешних gates.
