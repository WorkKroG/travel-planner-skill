prompt_version: adversarial-v1
prompt: Deliberately omit the known last-admission conflict to prove this negative fixture remains a raw hard failure.
response: The mutation incorrectly omits the last-admission conflict.
operations: {checks: {OPS-002: {status: ignored}}}
