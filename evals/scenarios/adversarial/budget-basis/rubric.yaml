version: 1
dimensions:
  - id: skeleton_distinctness
    max_score: 4
    anchors: {required: ["scenario budget-basis"], evidence: ["mixed budget basis"], forbidden: ["budget basis aligned"]}
  - id: tradeoffs
    max_score: 4
    anchors: {required: ["normalize for 2 travellers"], evidence: ["900.00 EUR"], forbidden: ["add raw amounts"]}
  - id: pacing
    max_score: 4
    anchors: {required: ["before summing"], evidence: ["per person"], forbidden: ["summed without normalization"]}
  - id: backup_usefulness
    max_score: 4
    anchors: {required: ["show original quotes"], evidence: ["normalized group total"], forbidden: ["hide quote basis"]}
  - id: readability
    max_score: 4
    anchors: {required: ["hotel-quote"], evidence: ["pass-price"], forbidden: ["unidentified"]}
  - id: calibrated_uncertainty
    max_score: 4
    anchors: {required: ["frozen quotes only"], evidence: ["taxes remain as declared"], forbidden: ["all taxes verified"]}
