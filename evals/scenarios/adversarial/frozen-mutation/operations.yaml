prompt_version: adversarial-v1
prompt: Deliberately change a frozen hotel without impact analysis or confirmation to prove the hard gate remains raw failure.
response: The planted mutation silently changes a frozen hotel and is not identified.
operations:
  checks: {EVAL-FROZEN-001: {status: ignored}}
  effects: {forbidden_behaviors: []}
  mutation: {kind: frozen_hotel, affected_id: day-2-hotel}
