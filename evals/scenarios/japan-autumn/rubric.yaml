version: 1
dimensions:
  - id: skeleton_distinctness
    max_score: 4
    anchors: {required: ["scenario japan-autumn"], evidence: ["composite travel-plan challenge"], forbidden: ["all constraints clear"]}
  - id: tradeoffs
    max_score: 4
    anchors: {required: ["day-7-event expected Friday"], evidence: ["is Saturday"], forbidden: ["weekday matches"]}
  - id: pacing
    max_score: 4
    anchors: {required: ["regional schedule published=False"], evidence: ["2026-11-08"], forbidden: ["schedule published=True"]}
  - id: backup_usefulness
    max_score: 4
    anchors: {required: ["weather rain swaps day-7"], evidence: ["kanazawa-museum"], forbidden: ["outdoor backup"]}
  - id: readability
    max_score: 4
    anchors: {required: ["apply each computed hard constraint"], evidence: ["without hidden route edits"], forbidden: ["unidentified"]}
  - id: calibrated_uncertainty
    max_score: 4
    anchors: {required: ["frozen fixture evidence"], evidence: ["timings, authority states, and availability"], forbidden: ["live facts verified"]}
